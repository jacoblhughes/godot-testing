extends Area2D


# Called when the node enters the scene tree for the first time.
func _ready():
#	collision_shape = $CollisionShape2D
#	set_gravity_point_center(Vector2(collision_shape.shape.get_rect().size.x/2,collision_shape.shape.get_rect().size.y/2))
#	print(collision_shape.shape.get_rect().size.x/2)
#	var shape_center = collision_shape.get_shape().get_center()
#	set_gravity_point_center(shape_center)

	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
